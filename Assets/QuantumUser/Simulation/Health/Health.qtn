component Health
{
    [HideInInspector]
    FP Current;

    FP Max;
}

signal OnHealthChanged(entity_ref entity, FP changeDelta);
signal OnEntityDeath(entity_ref entity);

event EntityDeath
{
    entity_ref Entity;
}