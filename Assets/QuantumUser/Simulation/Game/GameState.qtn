global
{
    dictionary<player_ref, entity_ref> ActivePlayers;
    list<entity_ref> AlivePlayers;

    FP PlayerMoney;

    GameLocation SelectedLocation;

    list<entity_ref> TrackedQuotaZones;
    entity_ref TrackedInteractableMapChanger;

    RuntimeMapCustomData RuntimeCustomData;

    FP QuotaZoneCount;
}

signal OnCompleteAllQuotaZones();
signal OnMapChangeAvailable();

event PlayerMoneyUpdated { }

event MapChangedToHub { }
event MapChangedToProcedural { }