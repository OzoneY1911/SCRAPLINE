component PlayerMovement
{
    FP WalkSpeed;
    FP RunSpeed;
    FP CrouchSpeed;
    FP JumpForce;

    FP HeightStanding;
    FP HeightCrouching;
    FP CrouchLerpSpeed;

    [HideInInspector]
    Boolean IsCrouching;

    [HideInInspector]
    Boolean IsRunning;

    FP CameraStandHeight;
    FP CameraCrouchHeight;
}

signal OnPlayerJump(entity_ref entity);